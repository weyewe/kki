
<div class="span9 content">
  <%= create_guide("Finalize Group Loan", 
        "Finalize group loan for office: <b>#{@office.name}</b>" + "<br />" + 
        "Total Pending Approval Group Loan: <b>#{@pending_approval_group_loans.count}</b> ").html_safe %>
  
  <%#= create_breadcrumb(@breadcrumbs).html_safe %>
  
  
   
  <div class="row-fluid">
    <div id="data_list">
      <table cellpadding="0" cellspacing="0" border="0" 
            class="table table-striped table-bordered" id="example">
        <thead>
          <tr>
            <th>Nama Group</th>
            <th>Lokasi RW</th>
            <th>Total Member</th>
            <th>Aggregated Principal</th>
            <th>Aggregated Interest</th>
            <th>Aggregated Interest Rate</th>
       
            <th colspan="2" style="text-align:center;">Action</th>
            
          </tr>
        </thead>
        <tbody>
          <% @pending_approval_group_loans.each do |group_loan| %>
            <%= render :partial => "group_loans/group_loan_starter" , 
                :locals => {:group_loan => group_loan}%>
            
     <!--             <tr >
                   <td><%= link_to group_loan.name, new_group_loan_group_loan_subcription_url(group_loan) %></td>
                   <td><%= group_loan.get_commune %></td>
                   <td><%= group_loan.members.count %></td>
                   <td><%=  print_money( group_loan.aggregated_principal_amount ) %></td>
                   <td><%=  print_money( group_loan.aggregated_interest_amount ) %></td>
                   <td><%=  group_loan.aggregated_interest_rate_percentage.to_s + " %" %></td>
                   <td style="text-align:center;"><span class="label">Pending</span></td>
                   
                 </tr> -->
            
          <% end %>
       
          

        </tbody>
      </table>
    </div><!-- end of #data_list -->
  </div>
  
  <div class=" hidden" id="approve_propose_form_wrapper">
    <%= form_for GroupLoan.new, :url => execute_start_group_loan_url   do |f| %>
      <%= hidden_field_tag 'entity_id' , ''  %>
      <%= hidden_field_tag 'action_role', '' %>
      <%= hidden_field_tag 'action_value', '' %>
    <% end %>
  </div>
  
  
  
  <div class="modal" id="myModal" style="display:none;">
     <div class="modal-header">
       <a class="close" data-dismiss="modal">×</a>
       <h3>Modal header</h3>
     </div>
     <div class="modal-body">
       <p>One fine body…</p>
     </div>
     <div class="modal-footer">
       <a href="#" class="btn modal-closer">Close</a>
       <!-- <a href="#" class="btn btn-primary">Save changes</a> -->
     </div>
   </div>
   
   

</div><!--/span-->















