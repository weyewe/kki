
<div class="span9 content">
  <%= create_guide("#{t 'process.independent_payment'}", 
        "#{t 'process.select_group_loan'}").html_safe %>
  
  <%= create_breadcrumb(@breadcrumbs).html_safe %>
  
  
   
  <div class="row-fluid">
    <div id="data_list">
      <table cellpadding="0" cellspacing="0" border="0" 
            class="table table-striped table-bordered" id="example">
        <thead>
          <tr>
            <th><%= t 'report_metric.group_name' %></th>
            <th>Lokasi RW</th>
            <th>Total Member</th>
            <th><%= t 'report_metric.total_backlog' %></th>
            <th><%= t 'report_metric.cleared_backlog' %></th>
          </tr>
        </thead>
        <tbody>
          <% @running_group_loans.each do |group_loan| %>
            <%= render :partial => "group_loans/group_loan_with_backlog" , 
                :locals => {:group_loan => group_loan}%>
          <% end %>
        </tbody>
      </table>
    </div><!-- end of #data_list -->
  </div>
  
 
  

</div><!--/span-->















