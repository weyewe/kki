$(document).ready(function(){
  <% new_row =escape_javascript(  render :partial => "member_payments/independent_payment_entry" , 
      :locals => {:independent_payment => @transaction_activity,
	:weekly_task => }  ) %>

  $("#independent_payment_entry-<%= @transaction_activity.id %>").replaceWith('<%= new_row %>');
});
