$(document).ready(function(){
	  
	
	
	
		<% new_form = escape_javascript(  render :partial => "member_attendances/new_object_form", :locals => {
	    :new_object =>@member_attendance, 
			:url => update_member_attendance_url(@weekly_task, @member ),
			:member => @member
	  })%>
 
	$("#editEntry .modal-body").append('<%= new_form %>');
	
	jQuery('.prevent_double_submit').preventDoubleSubmit();
});