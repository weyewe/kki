
<div class="span9 content">
  <%= create_guide("Assign Employee to Group Loan", 
        "Group Loan ID: <b>#{@group_loan.name}</b>").html_safe %>
  
  <%= create_breadcrumb(@breadcrumbs).html_safe %>
  
  <!-- <hr /> -->
  
  
  <div class="row-fluid">
    <!-- <div class="span12"> -->
      
      <table cellpadding="0" cellspacing="0" border="0" 
            class="table table-striped table-bordered" id="example">
        <thead>
          <tr>
            <th>email</th>
            <th>Role</th>
            <th style="text-align:center;">Active Field Workers Assignment</th>
            <th style="text-align:center;">Active Loan Inspector Assignment</th>
            <th style="text-align:center;">Loan Inspector Assignment</th>
            <!-- <th style="text-align:center;">Loan Inspector Assignment</th> -->
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <% if  user.has_role?(:field_worker, user.active_job_attachment) %>
              <% next %>
            <% end %>
              
            <%= render :partial => "group_loan_assignments/loan_inspector_assignment" , :locals => {
              :user => user, 
              :group_loan => @group_loan
            }%>
            
          <% end %>


         
        </tbody>
      </table>
      
      
      
      <div class=" hidden" id="checkbox_form_wrapper">
        <%= form_for GroupLoanMembership.new, :url =>execute_loan_inspector_assignment_url   do |f| %>
          <%= hidden_field_tag 'membership_provider' , ''  %>
          <%= hidden_field_tag 'membership_consumer', '' %>
          <%= hidden_field_tag 'membership_decision', '' %>
        <% end %>
      </div>
      <!-- <div class="alert alert-success allert-message">
             <a class="close" href="#">&times;</a>
             <strong>Well done!</strong> You successfully read this important alert message.
           </div>

           
           <div class="form-actions">
             <%#= hidden_field_tag 'destination_url', 
                     group_group_memberships_url(@group) ,
                      :id => "destination_url" %>

             <button class="btn btn-large btn-primary"
                   type="submit" id="save_button">Save changes</button>
           </div> -->
     

     <div class="modal" id="myModal" style="display:none;">
       <div class="modal-header">
         <a class="close" data-dismiss="modal">×</a>
         <h3>Modal header</h3>
       </div>
       <div class="modal-body">
         <p>One fine body…</p>
       </div>
       <div class="modal-footer">
         <a href="#" class="btn modal-closer">Close</a>
         <!-- <a href="#" class="btn btn-primary">Save changes</a> -->
       </div>
     </div>
        
      
    <!-- </div> -->   <!--/span12-->
  </div><!--/row-->

</div><!--/span-->









