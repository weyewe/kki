require 'spec_helper'

describe User do
  context "user's operational" do 
    it 'must have at most one active job_attachment' 
    # so, there is no User.create
    # there is only office.create_user bla bla bla .. our API is bad.. but it works.. let it be 
    
    
  end
end
